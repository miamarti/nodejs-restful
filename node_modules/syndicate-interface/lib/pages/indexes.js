
var things = require('../things/ensure-indexes')
var mdb = require('../databases/mongodb')

var indexes = module.exports = []

indexes.push(things.slug('pages'))
indexes.push(things.name('pages'))

indexes.push(mdb.collection('pages').ensureIndex({
  email: 1
}, {
  sparse: true,
  unique: true,
  background: true
}))
