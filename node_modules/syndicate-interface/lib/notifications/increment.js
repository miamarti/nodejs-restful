
/**
 * Increment a user's notification count.
 */
 
var mdb = require('../databases/mongodb')

module.exports = function (user_ids) {
  return mdb.pages.find('_id', {
    $in: Array.isArray(user_ids) ? user_ids : [user_ids]
  }).inc('count.notifications')
}
